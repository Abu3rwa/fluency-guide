import 'package:flutter/material.dart';

class ListeningPracticeScreen extends StatelessWidget {
  const ListeningPracticeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Listening Practice'),
      ),
      body: const Center(
        child: const Text('Listening practice feature coming soon.'),
      ),
    );
  }
}
